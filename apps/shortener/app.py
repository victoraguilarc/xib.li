# -*- coding: utf-8 -*-

from django.apps import AppConfig


class ShortenerConfig(AppConfig):
    """Configuration for users and permissions functionalties."""
    name = 'apps.shortener'
    verbose_name = 'Shortener'
    verbose_name_plural = 'Shortener'
